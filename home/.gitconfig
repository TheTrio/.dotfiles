[user]
	email = 10794178+TheTrio@users.noreply.github.com 
	name = Shashwat
	signingkey = D832A282A5B2D7F0AE4AD2ABC8C0F67B133E6B15
[advice]
	pushNonFastForward = false
[core]
	sshCommand = ssh -i ~/.ssh/github -F /dev/null
[commit]
	gpgSign = true
[init]
	defaultBranch = main
[difftool]
	prompt = false
	trustExitCode = true
[merge]
  tool = code
  conflictstyle = diff3
[mergetool "code-insiders"]
  cmd = code --wait --merge $REMOTE $LOCAL $BASE $MERGED
[core]
    pager = delta
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
[diff]
    colorMoved = default
